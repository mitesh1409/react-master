# What is the main benefit of "Automatic Batching" in React 18?

It groups multiple state updates into a single re-render for better performance.

So instead of re-rendering a component for every state update,  
it will group all the state updates and re-render only once.
